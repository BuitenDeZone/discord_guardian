source 'https://rubygems.org'

# This is a discord bot you know...
gem 'discordrb'

gem 'elasticsearch', git: 'https://github.com/elasticsearch/elasticsearch-ruby.git'

# gem used to create the cli (and config parsing as a bonus)
gem 'cl'

group :development do
  # I'm a big fan of awesome_print.
  gem 'awesome_print'
end
